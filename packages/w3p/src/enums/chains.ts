export enum CHAIN_TYPES {
  EVM = 1,
  Solana = 2,
  Near = 3,
}
export enum SOLANA_CHAINS {
  DevNet = 'devnet',
  TestNet = 'testnet',
  MainNet = 'mainnet',
}

export enum NEAR_CHAINS {
  TestNet = 'testnet',
  MainNet = 'mainnet',
}
