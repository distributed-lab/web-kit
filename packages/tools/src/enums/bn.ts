export enum BN_ROUNDING {
  DEFAULT = 4,
  UP = 0,
  DOWN = 1,
  CEIL = 2,
  FLOOR = 3,
  HALF_UP = 4,
  HALF_DOWN = 5,
  HALF_CEIL = 7,
  HALF_FLOOR = 8,
}

export enum BN_ASSERT_DECIMALS_OP {
  LESS = 'LESS',
  GREATER = 'GREATER',
}
